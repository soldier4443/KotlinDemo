<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.turastory.kotlindemo">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">
    <activity android:name=".java.app.ResultActivity"/>
    <activity android:name=".kotlin.app.ResultActivity"/>
    <activity android:name=".java.app.UsingOverloads"/>
    <activity android:name=".kotlin.app.UsingOverloads"/>
  </application>
</manifest>
